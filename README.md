Whenever an AI agent is editing this code, always leave notes on code so a begginer can understand.
No bot command should ping the user when replying to them, when prefix and slash commands are ran, it should reply to the user silently without ping. 